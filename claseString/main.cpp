#include <iostream>
#include <stdlib.h>
#include <string.h>

using namespace std;
string cadena;

int main()
{

   cout<<"Ingresar la cadena ...";
   cin>>cadena;
   string subcadena(cadena,2,2);//de la pos 2 a la pos 6
   string cortar;
   cortar= cadena.substr(0,5);
   cout<<"valor de cortar "<< cortar<<"\n";
   cout<< tamano de la cadena "<<cadena.size()<<"\n";
   string insertados;
   insertado= cadena;


   insertado= cadena.insert(1,"juan"(;
   cout<<"valor de insertado "<<insertado<<"\n";
   string tree;
   ree="BBBBBBBBB";






}
